# invalid level throws an error

    Code
      clean_markdown("text", level = "extreme")
    Condition
      Error in `match.arg()`:
      ! 'arg' should be one of "basic", "moderate", "aggressive"

