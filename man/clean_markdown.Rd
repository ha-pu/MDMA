% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_markdown.R
\name{clean_markdown}
\alias{clean_markdown}
\title{Clean Markdown text for LLM consumption}
\usage{
clean_markdown(text, level = "basic")
}
\arguments{
\item{text}{\verb{[character]} A character string of Markdown content, such as
the output of \code{\link[=pdf_to_md]{pdf_to_md()}} or \code{\link[ragnar:read_as_markdown]{ragnar::read_as_markdown()}}. A character
vector of length greater than 1 is processed element by element with a
progress bar.}

\item{level}{\verb{[string]} Aggressiveness level: \code{"basic"}, \code{"moderate"}, or
\code{"aggressive"}. Each level includes all steps from the levels below it.
Defaults to \code{"basic"}.}
}
\value{
A cleaned character string, or a character vector of the same length
as \code{text} when \code{length(text) > 1}.
}
\description{
Applies a sequence of cleaning steps to Markdown text produced from PDF
conversion. Steps are grouped into three aggressiveness levels; each level
includes all steps from the levels below it.
}
\details{
\subsection{\code{"basic"} — high value, low risk}{
\itemize{
\item Remove isolated page numbers (lines containing only digits).
\item Remove table-of-contents leader lines (e.g. \verb{Introduction ......... 3}).
\item Repair soft-hyphenation at line breaks (\verb{algo-\\nrithm} → \code{algorithm}).
}
}

\subsection{\code{"moderate"} — medium value, some risk}{
\itemize{
\item Join hard-wrapped paragraph lines split by PDF column layout.
\item Remove repeated running headers/footers (lines appearing 3+ times that
are no longer than 80 characters).
\item Collapse redundant duplicate headings (consecutive identical headings, or
a plain-text title immediately preceding a matching heading).
}
}

\subsection{\code{"aggressive"} — targeted but potentially lossy}{
\itemize{
\item Remove "This page intentionally left blank" boilerplate.
\item Strip standalone copyright and DOI lines.
}

All levels also normalize line endings and collapse runs of three or more
consecutive blank lines to two.
}
}
\examples{
md <- "Introduction ......... 3\n\nalgo-\nrithm\n\n   42\n"
clean_markdown(md)

clean_markdown(md, level = "moderate")
}
