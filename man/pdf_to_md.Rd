% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdf_to_md.R
\name{pdf_to_md}
\alias{pdf_to_md}
\title{Convert a PDF file to Markdown}
\usage{
pdf_to_md(
  path,
  output = NULL,
  overwrite = FALSE,
  min_chars = 100L,
  language = "eng",
  dpi = 300L,
  clean = "basic",
  ...
)
}
\arguments{
\item{path}{\verb{[character]} Path to a PDF file, or a character vector of paths
to convert multiple files. When length is greater than 1, files are
converted in a for loop with a progress bar.}

\item{output}{\verb{[character]} Path for the output Markdown file, or a
character vector of the same length as \code{path}. Defaults to each \code{path}
with the \code{.pdf} extension replaced by \code{.md}.}

\item{overwrite}{\verb{[logical(1)]} If \code{FALSE} (default), an error is thrown
when \code{output} already exists.}

\item{min_chars}{\verb{[integer(1)]} Minimum number of non-whitespace characters
that must be present in the extracted text for OCR to be skipped.
Defaults to \code{100L}.}

\item{language}{\verb{[string]} Tesseract language code used when OCR is
triggered (e.g. \code{"eng"}, \code{"nld"}). Defaults to \code{"eng"}.}

\item{dpi}{\verb{[integer(1)]} Resolution used when rendering PDF pages to images
for OCR. Higher values improve accuracy at the cost of speed. Defaults to
\code{300L}.}

\item{clean}{\verb{[string]} Aggressiveness level passed to \code{\link[=clean_markdown]{clean_markdown()}}.
One of \code{"basic"} (default), \code{"moderate"}, \code{"aggressive"}, or \code{"none"} to
skip cleaning entirely.}

\item{...}{Passed on to \code{\link[ragnar:read_as_markdown]{ragnar::read_as_markdown()}} when single-column
layout is detected or \code{pdftools} is unavailable.}
}
\value{
The output file path invisibly, or a character vector of output
paths when \code{length(path) > 1}.
}
\description{
Converts a PDF file to Markdown and writes the result to a \code{.md} file. By
default the output is placed in the same directory as the input with the
extension replaced.
}
\details{
When the \pkg{pdftools} package is installed, the PDF is first checked for
a two-column layout using word bounding-box coordinates. If a two-column
structure is detected, text is extracted column by column via
\code{\link[pdftools:pdf_data]{pdftools::pdf_data()}} so that column order is preserved. Otherwise,
\code{\link[ragnar:read_as_markdown]{ragnar::read_as_markdown()}} is used.

If the extracted text contains fewer than \code{min_chars} non-whitespace
characters, or if it contains more than 10 CID font artefacts
(\code{(cid:N)} patterns produced by unresolvable font encodings), the PDF is
assumed to require OCR and is processed via the
\href{https://cran.r-project.org/package=tesseract}{tesseract} and
\href{https://cran.r-project.org/package=pdftools}{pdftools} packages, which
must be installed separately.
}
\examples{
\dontrun{
pdf_to_md("report.pdf")
pdf_to_md("report.pdf", output = "llm_ready/report.md")
pdf_to_md("report.pdf", output = "report.md", overwrite = TRUE)
pdf_to_md("scan.pdf", language = "nld")
pdf_to_md("report.pdf", clean = "moderate")
pdf_to_md("report.pdf", clean = "none")
}
}
